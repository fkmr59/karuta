<%= form_with(model: user_match) do |form| %>
  <% if user_match.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(user_match.errors.count, "error") %> prohibited this user_match from being saved:</h2>

      <ul>
        <% user_match.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.label :match_id, style: "display: block" %>
    <%= form.text_field :match_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
